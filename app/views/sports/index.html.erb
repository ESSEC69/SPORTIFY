<div class="container sports-container">
  <div class="row text-center">
    <h1>Sports</h1>
    <% @sports.each do |sport| %>
	    <div class="item">
			<a href="/sports/<%= sport.id %>"> <%= sport.name %> <%= image_tag "#{sport.id}/logo.png" %> </a>
      	</div>
    <%end%>
  </div>
</div>